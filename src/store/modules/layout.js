import { layout } from '../../layout';
import {
	store
} from 'utils/';
//store.set('layout', layout);
const state = layout;
const mutations = {};
const actions = {};
export default {
	namespaced: true,
	state,
	mutations,
	actions
}